# Local config (visible name). Same keys as .env — edit one place or both (scraper.env wins).
# See .env.example for Apollo / Skrapp / OpenAPI / LNN and other options.

ATTACH_EXISTING_CHROME=0
REMOTE_DEBUG_PORT=9222
MAX_LEADS=20
MAX_LEADS_CAP=500
ASK_MAX_LEADS=0
DELAY_MIN=4
DELAY_MAX=9
LEADS_FILE=linkedin_leads.xlsx
AI_BACKEND=ollama
OLLAMA_BASE_URL=http://127.0.0.1:11434
OLLAMA_MODEL=qwen2.5:7b
SKIP_PREFLIGHT=0
MAX_BROWSER_TABS=3
